// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4.1
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "in_pixel"
#define AUTOTB_TVIN_in_pixel  "../tv/cdatafile/c.getPixelClassification_Stream.autotvin_in_pixel.dat"
// wrapc file define: "out_pixel"
#define AUTOTB_TVOUT_out_pixel  "../tv/cdatafile/c.getPixelClassification_Stream.autotvout_out_pixel.dat"
#define AUTOTB_TVIN_out_pixel  "../tv/cdatafile/c.getPixelClassification_Stream.autotvin_out_pixel.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "out_pixel"
#define AUTOTB_TVOUT_PC_out_pixel  "../tv/rtldatafile/rtl.getPixelClassification_Stream.autotvout_out_pixel.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			in_pixel_depth = 0;
			out_pixel_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{in_pixel " << in_pixel_depth << "}\n";
			total_list << "{out_pixel " << out_pixel_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int in_pixel_depth;
		int out_pixel_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};

extern void getPixelClassification_Stream (
int in_pixel,
int* out_pixel);

void AESL_WRAP_getPixelClassification_Stream (
int in_pixel,
int* out_pixel)
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "out_pixel"
		aesl_fh.read(AUTOTB_TVOUT_PC_out_pixel, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_out_pixel, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_out_pixel, AESL_token); // data

			sc_bv<32> *out_pixel_pc_buffer = new sc_bv<32>[1];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'out_pixel', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'out_pixel', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					out_pixel_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_out_pixel, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_out_pixel))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: out_pixel
				{
					// bitslice(31, 0)
					// {
						// celement: out_pixel(31, 0)
						// {
							sc_lv<32>* out_pixel_lv0_0_0_1 = new sc_lv<32>[1];
						// }
					// }

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: out_pixel(31, 0)
						{
							// carray: (0) => (0) @ (1)
							for (int i_0 = 0; i_0 <= 0; i_0 += 1)
							{
								if (&(out_pixel[0]) != NULL) // check the null address if the c port is array or others
								{
									out_pixel_lv0_0_0_1[hls_map_index].range(31, 0) = sc_bv<32>(out_pixel_pc_buffer[hls_map_index].range(31, 0));
									hls_map_index++;
								}
							}
						}
					}

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: out_pixel(31, 0)
						{
							// carray: (0) => (0) @ (1)
							for (int i_0 = 0; i_0 <= 0; i_0 += 1)
							{
								// sub                    : i_0
								// ori_name               : out_pixel[i_0]
								// sub_1st_elem           : 0
								// ori_name_1st_elem      : out_pixel[0]
								// output_left_conversion : out_pixel[i_0]
								// output_type_conversion : (out_pixel_lv0_0_0_1[hls_map_index]).to_uint64()
								if (&(out_pixel[0]) != NULL) // check the null address if the c port is array or others
								{
									out_pixel[i_0] = (out_pixel_lv0_0_0_1[hls_map_index]).to_uint64();
									hls_map_index++;
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] out_pixel_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "in_pixel"
		char* tvin_in_pixel = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_in_pixel);

		// "out_pixel"
		char* tvin_out_pixel = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_out_pixel);
		char* tvout_out_pixel = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_out_pixel);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_in_pixel, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_in_pixel, tvin_in_pixel);

		sc_bv<32> in_pixel_tvin_wrapc_buffer;

		// RTL Name: in_pixel
		{
			// bitslice(31, 0)
			{
				// celement: in_pixel(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : in_pixel
						// sub_1st_elem          : 
						// ori_name_1st_elem     : in_pixel
						// regulate_c_name       : in_pixel
						// input_type_conversion : in_pixel
						if (&(in_pixel) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> in_pixel_tmp_mem;
							in_pixel_tmp_mem = in_pixel;
							in_pixel_tvin_wrapc_buffer.range(31, 0) = in_pixel_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_in_pixel, "%s\n", (in_pixel_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_in_pixel, tvin_in_pixel);
		}

		tcl_file.set_num(1, &tcl_file.in_pixel_depth);
		sprintf(tvin_in_pixel, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_in_pixel, tvin_in_pixel);

		// [[transaction]]
		sprintf(tvin_out_pixel, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_out_pixel, tvin_out_pixel);

		sc_bv<32>* out_pixel_tvin_wrapc_buffer = new sc_bv<32>[1];

		// RTL Name: out_pixel
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: out_pixel(31, 0)
				{
					// carray: (0) => (0) @ (1)
					for (int i_0 = 0; i_0 <= 0; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : out_pixel[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : out_pixel[0]
						// regulate_c_name       : out_pixel
						// input_type_conversion : out_pixel[i_0]
						if (&(out_pixel[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> out_pixel_tmp_mem;
							out_pixel_tmp_mem = out_pixel[i_0];
							out_pixel_tvin_wrapc_buffer[hls_map_index].range(31, 0) = out_pixel_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_out_pixel, "%s\n", (out_pixel_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_out_pixel, tvin_out_pixel);
		}

		tcl_file.set_num(1, &tcl_file.out_pixel_depth);
		sprintf(tvin_out_pixel, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_out_pixel, tvin_out_pixel);

		// release memory allocation
		delete [] out_pixel_tvin_wrapc_buffer;

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		getPixelClassification_Stream(in_pixel, out_pixel);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_out_pixel, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_out_pixel, tvout_out_pixel);

		sc_bv<32>* out_pixel_tvout_wrapc_buffer = new sc_bv<32>[1];

		// RTL Name: out_pixel
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: out_pixel(31, 0)
				{
					// carray: (0) => (0) @ (1)
					for (int i_0 = 0; i_0 <= 0; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : out_pixel[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : out_pixel[0]
						// regulate_c_name       : out_pixel
						// input_type_conversion : out_pixel[i_0]
						if (&(out_pixel[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> out_pixel_tmp_mem;
							out_pixel_tmp_mem = out_pixel[i_0];
							out_pixel_tvout_wrapc_buffer[hls_map_index].range(31, 0) = out_pixel_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvout_out_pixel, "%s\n", (out_pixel_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_out_pixel, tvout_out_pixel);
		}

		tcl_file.set_num(1, &tcl_file.out_pixel_depth);
		sprintf(tvout_out_pixel, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_out_pixel, tvout_out_pixel);

		// release memory allocation
		delete [] out_pixel_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "in_pixel"
		delete [] tvin_in_pixel;
		// release memory allocation: "out_pixel"
		delete [] tvout_out_pixel;
		delete [] tvin_out_pixel;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

